# Configuration de l'Assistant Personnel
# Copier ce fichier en '.env' et remplir les valeurs

# ==================== AZURE OPENAI (GPT-4o) ====================
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_KEY=your-azure-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4o
AZURE_OPENAI_MODEL=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# ==================== PARAMETRES LLM ====================
# Temperature de reponse (0.6 = balance nuance/coherence pour conversations)
LLM_TEMPERATURE=0.6
# Nombre maximum de tokens en output (2000 pour reponses detaillees)
LLM_MAX_TOKENS=2000
# Nombre de messages de conversation a conserver en contexte (50 pour vraies conversations)
CONVERSATION_HISTORY_SIZE=50

# ==================== HOME ASSISTANT ====================
HA_URL=http://homeassistant.local:8123
HA_TOKEN=your-long-lived-access-token

# ==================== HARDWARE ACCELERATION ====================
# DEVICE options: cuda, cpu, auto
DEVICE=auto

# Nombre de workers pour Whisper (multi-threading)
WHISPER_WORKERS=8

# ==================== TTS (KOKORO PRIMARY + FALLBACKS) ====================
# Moteur TTS prefere: kokoro (haute qualite locale), piper, openai, fish, coqui
TTS_ENGINE=kokoro

# Kokoro TTS - voix francaise haute qualite (Apache 2.0, local, CPU OK)
# Voix FR: ff_siwis (femme), ff_alma (femme alt), fm_music (homme)
KOKORO_VOICE=ff_siwis
# Langue: f=francais, e=english, j=japanese, z=chinese
KOKORO_LANG=f
KOKORO_ENABLED=true

# Fish-Speech HTTP REST endpoint (optionnel, fallback reseau)
FISH_SPEECH_URL=http://localhost:8000

# Enable XTTS v2 fallback (local, GPU-ready, no network needed)
# Set to false if you don't want fallback TTS
TTS_FALLBACK=true

# XTTS v2 device (auto/cuda/cpu)
XTTS_DEVICE=auto

# TTS timeout and retry settings
TTS_TIMEOUT=30
TTS_RETRIES=2

# Fix telechargement HuggingFace sur Windows
HF_HUB_DISABLE_XET=1

# ==================== MOPIDY / TIDAL ====================
MOPIDY_URL=http://localhost:6680
TIDAL_QUALITY=LOSSLESS

# ==================== LOGGING ====================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR

# ==================== GUI ====================
GUI_WIDTH=800
GUI_HEIGHT=600
GUI_FPS=144
ENABLE_PYGAME=true

# ==================== WYOMING (Audio Protocol) ====================
WYOMING_HOST=0.0.0.0
WYOMING_PORT=10700

# ==================== DÉVELOPPEMENT ====================
DEBUG=false
MOCK_HA=false
